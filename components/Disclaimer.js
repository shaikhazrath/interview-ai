import React, { useState } from 'react';

const Disclaimer = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <div className="flex">
    
    </div>
  );
}

export default Disclaimer;
